@model ItemListViewModel

<div>
    <h2>@Model.Categoria.Nome</h2>
</div>

<div class="container">
    <div class="row">
        @foreach (var item in Model.Itens)
        {
           <partial name="_ItensResumo" for="@item"/>
        }
    </div>
</div>